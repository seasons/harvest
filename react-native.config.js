module.exports = {
  dependencies: {
    "react-native-config": {
      platforms: {
        ios: null,
      },
    },
  },
  project: {
    ios: {},
    android: {}, // grouped into "project"
  },
  assets: ["./assets/fonts/"], // stays the same
}
